[project]
name = "fpl-vault"
version = "0.1.0"
description = "Fantasy Premier League data ingestion and local database"
readme = "README.md"
requires-python = "~=3.14"
dependencies = [
    "duckdb>=1.4",
    "pytest>=9.0.2",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
    "requests>=2.33.1",
]
